# Статичні файли CRM

| Каталог | Призначення |
|---------|-------------|
| `brand/` | Логотип (`elfduck-logo.webp`) |
| `products/` | Превʼю товарів для аналітики (`*.webp`) |

Мапінг ключ → файл: `src/lib/productImages.js`.

Нові PNG: `./scripts/optimize-crm-images.sh` з кореня `elf-duck-shop` (потрібен `cwebp`).
